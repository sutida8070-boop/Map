<style>
    @import url('https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500;700&display=swap');

    body {
        font-family: 'Kanit', sans-serif;
    }

    #map {
        height: calc(100vh - 64px);
        z-index: 1;
    }

    /* ปรับแต่ง SweetAlert2 ให้เป็นฟอนต์ Kanit */
    .swal2-popup {
        font-family: 'Kanit', sans-serif !important;
    }

    /* ปรับแต่ง Popup บนแผนที่ */
    .leaflet-popup-content-wrapper {
        border-radius: 12px;
        padding: 5px;
    }

    .custom-input {
        @apply w-full mb-4 p-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:outline-none transition-all;
    }
</style>
